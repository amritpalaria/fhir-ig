Ok great. So here  is what I want you to do:
For each of the extensions listed in the second spreadsheet, I want you to create the fsh for the extension according to the x in value[x], url, applicability and the defintion listed in the sheet. Add the fsh in the fsh/extensions folder. These are some additional 314e extensions to be created and added to the 314e IG. To see how other extensions created by 314e have been given name, id, title, so that you are consistent with the existing pattern, you can refer to the existing fsh in the extensions folder.
Then I want you to go the first sheet and create the fsh for the 314e resource profiles listed there using the information provided. (The fsh for 314e created extensions are all in the fsh/extensions folder and for 314e profiled datatypes are in the fsh/datatypes folder.) The 314e created resource profile fsh you create should be added in the fsh/resources folder. To see how other resources created by 314e have been given name, id, title, so that you are consistent with the existing pattern, you can refer to the existing fsh in the resources folder.

Let's add any missing fsh for resources referenced from the resources we already have profiles for and then further any those are referenced from these new ones and so on until the set is fully closed. A resource needs to be 314e profiled so that its elements use the 314e defined datatypes and has 314e defined extensions. (It will mostly be all because there are 314e defined extensions for all resources.) Note that for all resources the parent should be QICore if a QICore profile exists for that resource type, if not, then it should be US Core profile if one exists and if not even that then finally the parent should come from FHIR R4B.